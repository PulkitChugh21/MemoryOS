# MemoryOS Backend — Phase 1 Dependencies
# Approved stack only (see Rules.md §1)

# --- Core Framework ---
fastapi==0.115.12
uvicorn[standard]==0.34.3
python-multipart==0.0.20

# --- Database (SQLAlchemy async + Alembic) ---
sqlalchemy[asyncio]==2.0.41
asyncpg==0.30.0
alembic==1.16.2
greenlet==3.2.3

# --- Auth (JWT + Password Hashing) ---
python-jose[cryptography]==3.4.0
passlib[bcrypt]==1.7.4
bcrypt==4.3.0

# --- LLM + Embeddings (Gemini API) ---
google-generativeai==0.8.5

# --- Vector DB (Qdrant) ---
qdrant-client==1.14.2

# --- Redis Cache (Upstash REST) ---
upstash-redis==1.2.0

# --- Token Counting ---
tiktoken==0.9.0

# --- Logging ---
structlog==25.4.0

# --- Utilities ---
pydantic==2.11.3
pydantic-settings==2.9.1
httpx==0.28.1
sse-starlette==2.3.6
email-validator==2.3.0

# --- Background Jobs (stub for Phase 1, full Celery in Phase 3) ---
# celery — deferred to Phase 3

# --- BM25 (stub for Phase 1, full hybrid search in Phase 2) ---
# rank-bm25 — deferred to Phase 2
